#ifndef ARPACK_DRV_BINDINGS_ARPACK_OVERLOADS_HPP
#define ARPACK_DRV_BINDINGS_ARPACK_OVERLOADS_HPP

#include "arpack_interface.h"

namespace arpack {
  namespace overloads {
    inline int aupd(FORTRAN_INT& ido, const char* bmat,
		    const FORTRAN_INT& n, const char *which,
		    const FORTRAN_INT& nev, 
  }
}

#endif
